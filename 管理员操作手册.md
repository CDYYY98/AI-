# AI 小说创作工作台 — 管理员操作手册

## 一、系统架构

```
用户电脑                        你的云服务器
┌──────────────┐              ┌─────────────────────┐
│ 桌面 EXE     │───HTTPS────→│ 后端 API (端口 3000) │
│ (免安装)     │              │ 数据库 (SQLite)      │
│              │              │ AI 模型调用          │
│ 用户只管写作  │              │ 你管理 API Key       │
└──────────────┘              └─────────────────────┘
```

---

## 二、服务器日常管理

### 登录服务器

```bash
ssh root@你的服务器IP
```

### 查看运行状态

```bash
pm2 status
```

### 查看日志

```bash
pm2 logs ai-novel         # 实时日志
pm2 logs --lines 50       # 最近 50 行
```

### 重启服务

```bash
pm2 restart ai-novel
```

### 停止服务

```bash
pm2 stop ai-novel
```

### 修改环境变量（改 API Key）

```bash
cd /root/ai-novel
vi .env
# 改完保存后重启
pm2 restart ai-novel
```

### 服务器重启后自动恢复

pm2 已配置开机自启。如果没生效：

```bash
pm2 startup
pm2 save
```

---

## 三、API Key 管理

### 初次配置

编辑服务器上的 `.env` 文件：

```ini
OPENAI_API_KEY=sk-xxxxxxxx
OPENAI_MODEL=gpt-5-mini

DEEPSEEK_API_KEY=sk-xxxxxxxx
DEEPSEEK_MODEL=deepseek-chat

ANTHROPIC_API_KEY=sk-ant-xxxxxxxx
ANTHROPIC_MODEL=claude-sonnet-4-6
```

改完重启：`pm2 restart ai-novel`

### 后续修改

SSH 上去改 `.env` → `pm2 restart ai-novel`，或者通过浏览器登录管理员账号在后台修改。

---

## 四、用户管理

### 查看所有用户

浏览器访问：`http://你的服务器IP:3000/api/admin/users`

请求头需带认证 Token。

### 创建管理员账号

```bash
cd /root/ai-novel/server
ADMIN_EMAIL=admin@xxx.com ADMIN_PASSWORD=你的密码 npx ts-node src/scripts/createAdmin.ts
```

### 禁用用户

需要先登录管理员账号获取 Token，然后：

```bash
curl -X PATCH http://localhost:3000/api/admin/users/用户ID \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer 管理员Token" \
  -d '{"isActive": false}'
```

---

## 五、打包发布新版本

### 完整打包流程

**本地 PowerShell：**

```powershell
# 1. 修改代码后先编译
cd "D:\新建文件夹\AI-Novel-Writing-Assistant-main"
pnpm run build

# 2. 上传到服务器
cd "D:\新建文件夹"
tar -czf ai-novel.tar.gz --exclude=node_modules --exclude=desktop/build "AI-Novel-Writing-Assistant-main"
scp ai-novel.tar.gz root@你的服务器IP:/root/
```

**服务器上：**

```bash
cd /root
tar -xzf ai-novel.tar.gz
rm -rf ai-novel
mv AI-Novel-Writing-Assistant-main ai-novel
cd /root/ai-novel
pnpm install
cd server
npx prisma generate --config prisma.config.ts
pm2 restart ai-novel
```

### 打包新 EXE

**本地 PowerShell：**

```powershell
cd "D:\新建文件夹\AI-Novel-Writing-Assistant-main"
set AI_NOVEL_API_BASE_URL=http://你的服务器IP:3000/api
set AI_NOVEL_ALLOW_UNSIGNED_RELEASE=true
pnpm dist:desktop:portable
```

EXE 在：`desktop\build\dist\AI Novel Writing Assistant v2-版本号-portable-x64.exe`

### 分发

把 EXE 文件发给用户。用户下载后：
1. 双击运行
2. 看到登录页
3. 点注册，填信息
4. 登录后直接使用

---

## 六、常见问题

| 问题 | 解决 |
|------|------|
| 服务挂了 | `pm2 restart ai-novel` |
| 端口不通 | 阿里云安全组放行 3000 |
| 内存不足 | 服务器升级到 4G+ 内存 |
| API 调用失败 | 检查 `.env` 中 API Key |
| 用户无法登录 | 检查 `curl localhost:3000/api/health` |

---

## 七、重要文件路径

| 文件 | 路径 |
|------|------|
| 环境变量 | `/root/ai-novel/.env` |
| 数据库 | `/root/ai-novel/server/dev.db` |
| 日志 | `/root/.pm2/logs/` |
| 项目代码 | `/root/ai-novel/` |
